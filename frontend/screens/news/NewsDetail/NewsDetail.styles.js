import { StyleSheet } from 'react-native';
export const styles = StyleSheet.create({
    deleteButton: {
        backgroundColor: 'darkred',
        padding: 10,
        borderRadius: 5,
        alignItems: 'center',
    },
    deleteButtonText: {
        color: 'white',
        fontWeight: 'bold',
        fontSize: 16,
    },
    title: {
        fontSize: 22,
        marginTop: 5,
        fontWeight: 'bold',
        textAlign: 'center',
    }
});